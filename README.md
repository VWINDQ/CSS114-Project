## 📋 ฟีเจอร์หลัก (Features)

โปรแกรมนี้สามารถดำเนินการคำนวณทางคณิตศาสตร์ได้ดังนี้:

* **Gaussian Elimination:** ใช้สำหรับการแก้ระบบสมการเชิงเส้น ($Ax = b$)
* **Gauss-Jordan Elimination:** ใช้สำหรับหา **Inverse Matrix ($A^{-1}$)**
* **LU Decomposition (Doolittle's Algorithm):** แยกเมทริกซ์เป็น Lower และ Upper triangular matrix (กำลังพัฒนา)
* **Matrix Operations:** การจัดการเมทริกซ์พื้นฐาน การแสดงผลรูปแบบตารางที่อ่านง่าย

## 🛠️ โครงสร้างของโค้ด (Algorithm Details)

โปรแกรมใช้เทคนิค **Augmented Matrix** ในการหา Inverse โดยมีขั้นตอนการทำงานหลักคือ:
1.  สร้างเมทริกซ์แต่งเติม $[A | I]$ ขนาด $N \times 2N$
2.  ดำเนินการ **Row Operations** เพื่อแปลงฝั่งซ้ายให้เป็น Identity Matrix ($I$)
    * **Normalization:** ปรับ Pivot ให้เป็น 1
    * **Elimination:** กำจัดตัวแปรอื่นๆ ในหลักเดียวกันให้เป็น 0
3.  ผลลัพธ์ที่ได้ในฝั่งขวาจะเป็น Inverse Matrix ($A^{-1}$)
4.  มีการจัดการค่า **Negative Zero (-0.00)** เพื่อความสวยงามและความถูกต้องของการแสดงผล

## 🚀 วิธีการติดตั้งและรันโปรแกรม (Getting Started)

### Prerequisites
* GCC Compiler (หรือ C Compiler อื่นๆ)

### Installation & Execution

1.  **Clone repository**
    ```bash
    git clone [https://github.com/VWINDQ/CSS114-Project.git](https://github.com/VWINDQ/CSS114-Project.git)
    cd CSS114-Project
    ```

2.  **Compile code**
    ```bash
    gcc GEM.c -o GEM
    ```

3.  **Run program**
    * **Mac/Linux:**
        ```bash
        ./GEM
        ```
    * **Windows:**
        ```bash
        GEM.exe
        ```

## 📊 ตัวอย่างการใช้งาน (Example)

**Input Matrix (3x3):**
```text
 2   1  -1
-3  -1   2
-2   1   2