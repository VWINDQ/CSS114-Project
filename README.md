## 🧮 อัลกอริทึมที่ใช้ (Algorithms Used)

โปรเจกต์นี้รวบรวม 4 วิธีหลักในการแก้ปัญหาพีชคณิตเชิงเส้น (Linear Algebra) โดยมีหลักการทำงานดังนี้:

### 1. Gaussian Elimination
ใช้วิธีการกำจัดตัวแปร (Elimination) เพื่อเปลี่ยนเมทริกซ์ให้อยู่ในรูปแบบ **Row Echelon Form (สามเหลี่ยมบน)**
* **Step 1:** ใช้ Row Operations กำจัดตัวเลขใต้เส้นทแยงมุมให้เป็น 0 ทั้งหมด
* **Step 2:** จะได้สมการในรูปสามเหลี่ยมบน (Upper Triangular Matrix)
* **Step 3:** ทำ **Backward Substitution** (แทนค่าจากสมการล่างสุดขึ้นบน) เพื่อหาคำตอบของระบบสมการ

### 2. Gauss-Jordan Elimination
เป็นวิธีการขยายต่อจาก Gaussian Elimination เพื่อทำให้อยู่ในรูป **Reduced Row Echelon Form (RREF)**
* **Step 1:** ปรับ Pivot ในแนวทแยงให้เป็น 1 (Normalization)
* **Step 2:** กำจัดตัวเลข **ทั้งเหนือและใต้** เส้นทแยงมุมให้เป็น 0
* **Step 3:** เมทริกซ์ผลลัพธ์จะเป็นเมทริกซ์เอกลักษณ์ (Identity Matrix) ซึ่งสามารถอ่านค่าคำตอบของตัวแปรได้ทันทีโดยไม่ต้องแทนค่าย้อนกลับ

### 3. LU Decomposition (Doolittle's Method)
แยกเมทริกซ์ $A$ ออกเป็นสองส่วนคือ Lower ($L$) และ Upper ($U$) เพื่อประสิทธิภาพในการแก้สมการซ้ำๆ
* **Step 1:** แยก $A = L \times U$ โดยกำหนดให้แนวทแยงมุมของ $L$ เป็น 1
* **Step 2:** ใช้ $L$ และ $U$ ในการแก้สมการ 2 ขั้นตอน:
    1.  หา $y$ จาก $Ly = b$ (Forward Substitution)
    2.  หา $x$ จาก $Ux = y$ (Backward Substitution)

### 4. Matrix Inversion (Augmented Matrix Method)
การหาอินเวอร์สของเมทริกซ์โดยใช้หลักการ **Augmented Matrix $[A | I]$** ร่วมกับ Gauss-Jordan
* **Step 1:** สร้างเมทริกซ์แต่งเติมขนาด $N \times 2N$ โดยฝั่งซ้ายเป็นเมทริกซ์ $A$ และฝั่งขวาเป็น Identity Matrix ($I$)
* **Step 2:** ดำเนินการ Row Operations เพื่อแปลงฝั่งซ้าย ($A$) ให้กลายเป็น Identity Matrix ($I$)
* **Step 3:** เมื่อฝั่งซ้ายเป็น $I$ แล้ว เมทริกซ์ฝั่งขวาจะกลายเป็น **Inverse Matrix ($A^{-1}$)** โดยอัตโนมัติ